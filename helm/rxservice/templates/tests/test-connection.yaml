apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rxservice.fullname" . }}-test-connection"
  labels:
    {{- include "rxservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rxservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
