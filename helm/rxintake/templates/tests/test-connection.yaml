apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rxintake.fullname" . }}-test-connection"
  labels:
    {{- include "rxintake.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rxintake.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
