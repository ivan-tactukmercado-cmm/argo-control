apiVersion: v1
kind: Service
metadata:
  name: {{ include "rxintake.fullname" . }}
  labels:
    {{- include "rxintake.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rxintake.selectorLabels" . | nindent 4 }}
